[package]
name = "drift-plugin"
version = "0.1.0"
edition = "2021"
description = "Pure storage policies and shared playlist types for Drift"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "drift_plugin"
path = "src/lib.rs"
